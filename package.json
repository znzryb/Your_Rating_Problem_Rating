{
  "name": "your-rating-problem-rating",
  "version": "0.1.0",
  "description": "Chrome extension: Carrot-style reverse-Elo problem rating for Codeforces, shown directly on the problem page.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && rsync -a --delete dist/ ~/chrome-extensions/your-rating-problem-rating/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
